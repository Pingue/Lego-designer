# Kaggle credentials now live in kaggle.json at the project root, not here.
#   cp kaggle.json.example kaggle.json   # then fill in username + token
#
# (Advanced) As an alternative to kaggle.json, you can instead set:
#   KAGGLE_USERNAME=your_username
#   KAGGLE_KEY=your_token
