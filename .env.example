# Kaggle credentials — needed only for downloading the dataset
# Copy the value Kaggle gives you at https://www.kaggle.com/settings → API
# Paste just the JSON part (without "export KAGGLE_API_TOKEN="):
KAGGLE_API_TOKEN={"username":"your_username","key":"your_api_key"}
